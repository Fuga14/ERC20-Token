// Mocha, Chai
const { expect, assert } = require('chai');
const { ethers } = require('hardhat');

describe('GryvnaToken contract', function () {
  let Token, gryvnaToken, owner, address1, address2;
  let tokenCap = 1000000;
  let tokenBlockReward = 50;
});
